#include <fstream>
#include <iostream>
#define maxn 110
using namespace std;

int n,mp[maxn][maxn],color[]={0,1,2,3};

int main() {
    ios::sync_with_stdio(0);
    cin>>n;
    for(int i=1;i<=n*n;i++){
        int a;
        cin>>a;
        
    }
    return 0;
}

