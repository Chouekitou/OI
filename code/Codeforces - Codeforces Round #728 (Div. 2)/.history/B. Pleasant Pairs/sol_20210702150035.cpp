#include <fstream>
#include <iostream>
#define maxn 200010
using namespace std;

int a[maxn];

void solve() {
    int n;
    cin >> n;
    for (int i = 1;i <= n;i++) {
        cin >> a[i];
    }
    int ans = 0;
    for (int i = 1;i <= n;i++) {

    }
}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

