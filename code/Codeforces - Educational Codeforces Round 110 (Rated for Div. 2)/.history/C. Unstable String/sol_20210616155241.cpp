#include <fstream>
#include <iostream>
#include <string>
using namespace std;

int k, q;
string s;

int main() {
    ios::sync_with_stdio(0);
    cin >> k;
    cin >> s;
    cin >> q;
    for (int i = 1;i <= q;i++) {
        int p;
        char c;
        cin >> p >> c;

    }
    return 0;
}

