#include <fstream>
#include <iostream>
#include <cmath>
#define ll long long
using namespace std;

void solve() {
    int n;
    cin >> n;
    for (int i = 1;i <= 9;i++) {
        for (int j = 0;j <= (int);)
    }
}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

