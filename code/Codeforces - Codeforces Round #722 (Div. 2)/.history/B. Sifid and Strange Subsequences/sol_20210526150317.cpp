#include <fstream>
#include <iostream>
#include <algorithm>
#define maxn 100010
using namespace std;

int n, a[maxn];

void solve() {
    cin >> n;
    for (int i = 1;i <= n;i++) {
        cin >> a[i];
    }
    sort(a + 1, a + 1 + n);
    int count = 0;
    for (int i = 1;i <= n;i++) {
        if ()
    }
}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

