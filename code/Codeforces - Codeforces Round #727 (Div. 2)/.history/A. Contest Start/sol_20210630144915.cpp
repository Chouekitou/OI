#include <fstream>
#include <iostream>
using namespace std;

void solve() {
    int n, x, t;
    cin >> n >> x >> t;

}

int main() {
    ios::sync_with_stdio(0);
    int k;
    cin >> k;
    while (k--) {
        solve();
    }
    return 0;
}

