#include <fstream>
#include <iostream>
#define maxn 2010
#define ll long long
using namespace std;

ll n, a[maxn];

void solve() {
    ll hp = 0;
    cin >> n;
    for (int i = 1;i <= n;i++) {
        cin >> a[i];
    }
    for (int i = 1;i <= n;i++) {

    }
}

int main() {
    ios::sync_with_stdio(0);
    solve();
    return 0;
}

