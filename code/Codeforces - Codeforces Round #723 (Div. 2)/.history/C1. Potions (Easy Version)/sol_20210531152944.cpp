#include <fstream>
#include <iostream>
#include <cstring>
#include <queue>
#define maxn 2010
#define ll long long
using namespace std;

ll n, a[maxn];

void solve() {
    cin >> n;
    priority_queue<ll, vector<ll>, greater<ll>>pq;
    ll s = 0;
    for (int i = 1;i <= n;i++) {
        ll x;
        cin >> x;

    }
}

int main() {
    ios::sync_with_stdio(0);
    solve();
    return 0;
}

