#include <fstream>
#include <iostream>
#include <vector>
#include <algorithm>
#include <stack>
#define maxn 300010
using namespace std;

struct bot {
    int x, d;
};

void solve() {
    int n, m;
    cin >> n >> m;
    for (int i = 1;i <= n;i++) {

    }
}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

