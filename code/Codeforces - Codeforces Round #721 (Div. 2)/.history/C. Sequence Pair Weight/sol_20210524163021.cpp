#include <fstream>
#include <iostream>
#include <map>
#define ll long long
#define maxn 100010
using namespace std;

map<int, int> count;
ll a[maxn];

void solve() {
    count.clear();
    int n;
    cin >> n;
    for (int i = 1;i <= n;i++) {
        cin >> a[i];

    }
}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

