#include <fstream>
#include <iostream>
using namespace std;

int n;

void solve() {
    cin >> n;
}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

