#include <fstream>
#include <iostream>
#include <algorithm>
#define maxn 2000
using namespace std;

void solve() {
    char a[maxn];
    int n;
    cin >> n;
    for (int i = 1;i <= n;i++) {
        cin >> a[i];
    }

}

int main() {
    ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}

