#include <fstream>
#include <iostream>
#include <vector>
#define maxn 300010
using namespace std;

int t,a[maxn],b[maxn];

void solve(){
    vector<int> sposa,sposb;
    int n;
    int suma0=0,suma1=0,sumb0=0,sumb1=0;
    for(int i=1;i<=n;i++){
        cin>>a[i];
        if()
    }
    for(int i=1;i<=n;i++){
        cin>>b[i];
    }

}

int main() {
    ios::sync_with_stdio(0);
    cin>>t;
    while(t--){
        solve();
    }
    return 0;
}

